<?php
$hasOtherSamples = false; // Whether has samples labeled by AI models as "other"
foreach ($results as $item) {
    foreach ($item as $key=>$value) {
        if (str_ends_with($key, '_result') && $value === 'other') {
            $hasOtherSamples = true;
            break;
        }
    }
}
?>
<div class="footnotes my-2 text-muted small">
    <div class="d-inline-block pt-2 border-top">
        <?php foreach (RESULTS_MODELS as $model=>$modelName) { ?>
            <p class="mb-0"><?= strtoupper($model) ?>: <?= $modelName ?></p>
        <?php } ?>
        <?php if ($hasOtherSamples) { ?><p id="footnote-other" class="mb-0">*: Neither RT027 nor RT181</p><?php } ?>
    </div>
</div>
